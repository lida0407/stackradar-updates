# StackRadar Updates

Public update feed for the StackRadar Android demo.

- `update-manifest.json` tells the app which APK and database version are current.
- `catalog.json` is the remotely updateable technology catalog.
- APK files are distributed through GitHub Releases.

The catalog entries here are seed/demo records and should be replaced with sourced research data before production use.
